# ==== Configuration File (.env) ====

# Paths to include in backups. Separate multiple entries using commas.
# Example: "/etc,/var/www,/home/user"
BACKUP_SOURCES="/etc,/home"

# Directory where backup archives will be written.
# Ensure the location exists and is writable.
BACKUP_TARGET="/var/backups"

# Number of days to retain old backup archives.
KEEP_DAYS=7

# Log Alerting
# Keywords to look for inside logs (case-insensitive).
ALERT_KEYWORDS="error,failed,panic,oom,kernel panic"

# Logs to scan. Leave blank to default to /var/log/syslog or journalctl.
SCAN_LOGS="/var/log/syslog"

# Minutes back to scan logs for alerts.
LOG_WINDOW_MINUTES=15

# Optional email address for alert notifications.
# Leave blank to disable notifications.
NOTIFY_EMAIL=""

# Force a specific package manager (apt, dnf, pacman).
# Leave blank for automatic detection.
PACKAGE_MANAGER=""
